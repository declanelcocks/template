import React from 'react'
import { SignupButton, UserButton } from 'containers'

const HomePage = () => {
  return (
    <div>
      <SignupButton />
      <UserButton />
    </div>
  )
}

export default HomePage
